name: Black Formatter (deprecated)

# This workflow has been consolidated into code-quality.yml.
# It is kept here only to avoid breaking any branch protection rules that
# reference the "format" job by name; the job is disabled via `if: false`.
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  format:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by code-quality.yml"
